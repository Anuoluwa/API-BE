// import log4js from 'log4js';
// import { config } from 'dotenv';
// config();

// log4js.configure({
//   appenders: { alertlog: { type: 'file', filename: process.env.logpath } },
//   categories: { default: { appenders: ['alertlog'], level: 'info' } }
// });

// export const logger = log4js.getLogger('alertlog');
